rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FlutterアプリはFunctions経由でのみアクセスする。
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
